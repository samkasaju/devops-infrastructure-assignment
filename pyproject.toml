[project]
name = "devops-trainee-assignment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Samriddhi Kasaju", email = "samkasaju@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.2.0",
    "psycopg2-binary>=2.9.13",
]

[project.scripts]
devops-trainee-assignment = "devops_trainee_assignment:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
